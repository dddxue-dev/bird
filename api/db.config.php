<?php
/**
 * ===============================================================
 *  数据库配置 —— 整个项目唯一需要按你本机情况修改的文件
 * ===============================================================
 *
 *  PHPStudy 用户请注意：
 *  这里填的账号密码，必须和你 PHPStudy 面板里「数据库」那一页显示的
 *  MySQL 账号密码完全一致。很多人装完 PHPStudy 后 root 密码并不是 root，
 *  而是安装时自己设置的（常见为 root / 123456 / 空）。
 *
 *  修改方法：改完保存，刷新页面即可，无需重启 Apache。
 *
 *  如果你不确定密码是什么：
 *    - 打开 PHPStudy 面板 -> 数据库 -> 看 root 的密码；
 *    - 或者直接访问 http://你的站点/setup.php 用图形界面测试并写入。
 *
 *  也可以不改本文件，改用环境变量覆盖（php.ini / Apache SetEnv 都行）：
 *    DB_HOST / DB_PORT / DB_USER / DB_PASS / DB_NAME
 */

return array(

    // 数据库地址。建议用 127.0.0.1（走 TCP），不要写 localhost
    'host' => '127.0.0.1',

    // 端口，MySQL 默认 3306
    'port' => '3306',

    // 数据库账号
    'user' => 'root',

    // 数据库密码  ←←← 出问题基本都是这里，改成你自己 PHPStudy 里的密码
    'pass' => 'root',

    // 数据库名。不存在的话，程序会自动创建
    'name' => 'wings',

    // 连接失败时，是否自动尝试几个常见的本地密码（空 / root / 123456 ...）
    // 方便本地调试；正式部署建议改为 false
    'auto_try_common_passwords' => true,
);
