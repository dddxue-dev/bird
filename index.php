<?php
/**
 * 兼容入口
 * ---------------------------------------------------------------
 * 本项目是「前后端分离」结构：前端是纯静态页面（index.html 等），
 * 后端是 api/ 下的 PHP 接口。
 *
 * 但有些 Apache / Nginx 配置的目录索引顺序是 index.php 优先，
 * 为了访问站点根目录时也能正确打开首页，保留这个极简转发入口。
 * 如果你确认服务器目录索引优先 index.html，可以直接删掉本文件。
 */
header('Location: index.html', true, 302);
exit;
